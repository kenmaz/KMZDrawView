#include "Pods-KMZDrawView.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/KMZDrawView" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/KMZDrawView"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}